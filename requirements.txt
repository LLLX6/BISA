# Server-side image decoding, EXIF normalization and bounded WebP output.
Pillow==12.3.0
# Standards-based Web Push with VAPID. The adapter remains disabled unless the
# complete BISA_VAPID_* credential group is present and internally consistent.
pywebpush==2.3.0
